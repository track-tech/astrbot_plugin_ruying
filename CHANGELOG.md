# 更新日志

所有重要变更记录在此文件中。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.3.11] - 2026-09-13

### 新增（隐身模式）
- `/如影 stealth on|off`（仅管理员）：操作手机时屏幕全黑——关闭自动亮度并把亮度置 0，
  系统与触控正常运行，肉眼看不到画面；截屏/看屏不受影响（截的是画面不是背光）
- 原始亮度自动记录并持久化，`off` 或插件卸载/重载时精确还原，避免忘关导致手机一直黑屏
- 已知边界：部分系统（如 ColorOS）钳制最低亮度，可能保留极微弱微光；有锁屏密码的设备
  唤醒后仍停在锁屏页

## [0.3.10] - 2026-09-13

### 修复
- 子 agent 模型商识别兼容新版 AstrBot：新版 Provider 实例的 ID 在 `meta().id`、
  模型名走 `get_model()`，旧式直接属性读取全部失败，导致「可用的模型商列表为空」、
  子 agent 无法派出。现抽取 `_provider_id` / `_provider_model` 跨版本兼容读取
  （meta().id → pv.id → provider_config；get_model() → model_name → model），
  `ruying_auto` 与 providers/agent_provider 指令均恢复可用

## [0.3.9] - 2026-09-12

### 新增（子 agent）
- `ruying_auto(task)` 工具：把多步手机操控任务交给「如影」子 agent 自主完成。
  子 agent 拥有独立上下文（工具结果不污染主对话历史、15 个工具 schema 不再常驻主对话），
  带专属安卓操控守则（先看屏再点、tap_and_wait 确认、digest 优先等），只回最终结果与用量
- **子 agent 模型可自定义**：从已配置的模型商中任选（建议带视觉能力的模型）；
  默认跟随会话当前模型。`/如影 providers` 查看列表，`/如影 agent_provider <ID|清空>` 切换
- 新增配置：`subagent_enabled` / `subagent_provider_id` / `subagent_max_steps`（默认 15）
- 单步操作仍建议直接用对应单步工具（handoff 有冷启动开销），工具描述已引导模型自行判断

## [0.3.8] - 2026-09-12

### 新增
- `keep_downloads`（默认 20）：pull 拉取的文件自动清理，downloads 目录只保留最近 N 个，
  与截图的 `keep_screenshots` 策略对称

## [0.3.7] - 2026-09-12

### 新增（省 token 优化）
- 截图回传压缩：默认降采样为长边 720 的 JPEG（质量 70）回传给 LLM，本地保留原始 PNG；
  真机实测 1080x2376 截图从 4.6MB PNG 降至 58KB JPEG（-99%）。新增配置
  `shot_max_edge` / `shot_quality`，工具参数 `max_edge` / `quality` / `region` 可临时覆盖
- 截图去重 `shot_dedup`：无操作间隔内重复截图且内容未变时只回文字「屏幕未变化」，`force=true` 强制回图
- `ruying_screenshot` 新增 `digest=true`：不回图，只返回界面元素文字摘要
- 新增原子工具 `ruying_tap_and_wait`：点击后轮询界面至稳定，返回变化摘要（新增/消失元素），
  替代「点击→截屏→看图」
- 新增原子工具 `ruying_wait_for`：轮询等待指定文字/资源 ID 出现，替代反复截图轮询
- `ruying_get_ui` 参数化：`max_nodes`（默认 60）数量上限、`filter_kw` 关键词过滤

### 优化
- 触控/输入/启动/按键/shell 操作后记录操作标记，配合截图去重

### 依赖
- 新增 `Pillow`（AstrBot 核心自带；缺失时截图降级回传原 PNG，功能不受影响）

## [0.3.6] - 2026-09-12

### 新增
- `auto_wake`（默认开启）：截屏 / 读控件 / 触控 / 输入 / 启动应用前自动检测屏幕状态，
  熄屏时自动唤醒（WAKEUP + 上滑尝试过锁屏）。熄屏时截屏为纯黑图，真机实测发现并修复。
- 新增配置 `auto_wake`

### 说明
- 设备设置了锁屏密码时，自动唤醒只能到锁屏页，LLM 看到锁屏截图后会提示用户；
  无法也不应远程绕过锁屏密码

## [0.3.5] - 2026-09-12

### 新增
- 添加 `CHANGELOG.md` 更新日志文件（即本文件）

## [0.3.4] - 2026-09-12

### 修复
- `adb_path` / `command_timeout` 改为惰性读取：**WebUI 修改配置保存后立即生效，无需重载插件**
- 「找不到 adb」报错现在附带当前配置值与三种生效时机的说明，便于定位配置未生效的场景

### 说明
- 重载插件若发生在同一轮对话进行中，该轮使用的工具集在开始时已快照（AstrBot 核心层行为），
  仍会调用旧插件实例；新开一轮对话即恢复

## [0.3.3] - 2026-09-12

### 变更
- Logo 重绘为现代矢量图标风格（细描边 / 圆头 / 扁平化），并附 `logo.svg` 矢量源文件

## [0.3.2] - 2026-09-12

### 新增
- 添加插件 Logo（手机 × 影子机器人 × 无线信号）

## [0.3.1] - 2026-09-12

### 修复
- `main.py` 内 `PLUGIN_VERSION` 常量与 `metadata.yaml` 版本号保持一致
- 截图文件名改用纳秒时间戳，避免同一秒内连续截屏互相覆盖
- `/如影 pull` 与 `ruying_pull_file` 对异常设备路径（空 / `.` / `..`）做兜底，防止文件落到下载目录之外
- 清理 `_ensure_online` 中冗余的异常重抛

## [0.3.0] - 2026-09-12

### 新增
- LLM 工具 `ruying_scan_devices`：可通过自然语言触发局域网扫描与自动连接（仅管理员），
  与 `/如影 scan` 共用同一条扫描-连接-登记链路

## [0.2.0] - 2026-09-12

### 新增
- `/如影 scan`（中文别名：扫描）：三层策略发现局域网设备并自动连接——
  zeroconf 直查 Android 11+ 无线调试 mDNS 广播、adb 自带 `mdns services`、经典 5555 端口扫描
- 自动登记：按机型起别名（重名自动加后缀）、同 IP 端口变化自动更新、未设默认时自动指定
- 扫描报告区分：新连接 / 已在线 / 连接失败 / 已连接待授权 / 处于配对模式的设备（附 pair 指引）
- 新增依赖 `zeroconf`（缺失时自动降级为 adb mDNS + 端口扫描）；新增配置 `scan_port_5555`

## [0.1.0] - 2026-09-12

### 新增
- 首个版本
- 无线调试全流程：`/如影 pair` 配对 → `/如影 connect` 连接登记，多设备管理
  （`devices` / `use` / `forget` / `disconnect` / `status`）
- 13 个 LLM 函数工具：截屏（多模态模型可直接看屏）、界面控件坐标、点击 / 滑动 / 输入文字 / 按键、
  应用查询与启动、文件拉取、设备状态、任意 shell（默认关闭）
- 操作前自动重连（含 mDNS 端口重发现），支持别名 / IP:端口 / 裸 IP 解析设备
- 权限体系：管理类仅管理员；操作类管理员 + 白名单会话；shell 双重开关
- 中文输入自动检测 ADBKeyboard，未安装时给出安装指引
- 兼容 AstrBot v4.28.0（源码级核验）

# 更新日志

所有重要变更记录在此文件中。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.3.5] - 2026-09-12

### 新增
- 添加 `CHANGELOG.md` 更新日志文件（即本文件）

## [0.3.4] - 2026-09-12

### 修复
- `adb_path` / `command_timeout` 改为惰性读取：**WebUI 修改配置保存后立即生效，无需重载插件**
- 「找不到 adb」报错现在附带当前配置值与三种生效时机的说明，便于定位配置未生效的场景

### 说明
- 重载插件若发生在同一轮对话进行中，该轮使用的工具集在开始时已快照（AstrBot 核心层行为），
  仍会调用旧插件实例；新开一轮对话即恢复

## [0.3.3] - 2026-09-12

### 变更
- Logo 重绘为现代矢量图标风格（细描边 / 圆头 / 扁平化），并附 `logo.svg` 矢量源文件

## [0.3.2] - 2026-09-12

### 新增
- 添加插件 Logo（手机 × 影子机器人 × 无线信号）

## [0.3.1] - 2026-09-12

### 修复
- `main.py` 内 `PLUGIN_VERSION` 常量与 `metadata.yaml` 版本号保持一致
- 截图文件名改用纳秒时间戳，避免同一秒内连续截屏互相覆盖
- `/如影 pull` 与 `ruying_pull_file` 对异常设备路径（空 / `.` / `..`）做兜底，防止文件落到下载目录之外
- 清理 `_ensure_online` 中冗余的异常重抛

## [0.3.0] - 2026-09-12

### 新增
- LLM 工具 `ruying_scan_devices`：可通过自然语言触发局域网扫描与自动连接（仅管理员），
  与 `/如影 scan` 共用同一条扫描-连接-登记链路

## [0.2.0] - 2026-09-12

### 新增
- `/如影 scan`（中文别名：扫描）：三层策略发现局域网设备并自动连接——
  zeroconf 直查 Android 11+ 无线调试 mDNS 广播、adb 自带 `mdns services`、经典 5555 端口扫描
- 自动登记：按机型起别名（重名自动加后缀）、同 IP 端口变化自动更新、未设默认时自动指定
- 扫描报告区分：新连接 / 已在线 / 连接失败 / 已连接待授权 / 处于配对模式的设备（附 pair 指引）
- 新增依赖 `zeroconf`（缺失时自动降级为 adb mDNS + 端口扫描）；新增配置 `scan_port_5555`

## [0.1.0] - 2026-09-12

### 新增
- 首个版本
- 无线调试全流程：`/如影 pair` 配对 → `/如影 connect` 连接登记，多设备管理
  （`devices` / `use` / `forget` / `disconnect` / `status`）
- 13 个 LLM 函数工具：截屏（多模态模型可直接看屏）、界面控件坐标、点击 / 滑动 / 输入文字 / 按键、
  应用查询与启动、文件拉取、设备状态、任意 shell（默认关闭）
- 操作前自动重连（含 mDNS 端口重发现），支持别名 / IP:端口 / 裸 IP 解析设备
- 权限体系：管理类仅管理员；操作类管理员 + 白名单会话；shell 双重开关
- 中文输入自动检测 ADBKeyboard，未安装时给出安装指引
- 兼容 AstrBot v4.28.0（源码级核验）
